US-scrape:
  image: openstates/scrapers
  entrypoint: "poetry run os-update usa bills --fast"
  enabled: true
  environment: scrapers
  triggers:
    - cron: 0 8 * * ?
  timeout_minutes: 720
  # next_tasks:
  #   - US-text

# US-text:
  # image: openstates/text-extraction
  # entrypoint: "poetry run ./text_extract.py update usa"
  # enabled: true
  # environment: scrapers
